{
  "name": "capacitor-android-template",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Capacitor Android build template \u2014 driven by GitHub Actions workflow_dispatch.",
  "scripts": {
    "patch": "node scripts/patch.mjs",
    "sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
